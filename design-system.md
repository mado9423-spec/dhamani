# نظام التصميم — تطبيق "ضماني"

جميع الألوان معرّفة كرموز (tokens) في `tailwind.config.js` — لا تُستخدم قيم hex مباشرة
داخل المكوّنات، بل الأصناف الدلالية أدناه (`bg-primary`, `text-ink`, إلخ) لضمان مصدر
واحد للحقيقة عبر التطبيق كاملاً.

## الألوان

| الاستخدام | الصنف (class) | القيمة |
|---|---|---|
| اللون الأساسي (كمّاني) | `primary` | `#123F63` |
| الأساسي عند hover | `primary-hover` | `#0B2D47` |
| الأساسي الغامق (شاشات الموظفين) | `primary-dark` | `#0A2436` |
| خلفية فاتحة للأساسي | `primary-light` | `#E8EEF4` |
| اللون المميز (ذهبي، للتنبيهات المعلّقة فقط) | `accent` / `accent-light` | `#B0820F` / `#FBF3E1` |
| نجاح | `success` / `success-light` | `#16803C` / `#EAF7F0` |
| خطأ | `danger` / `danger-light` | `#C0392B` / `#FBEAE8` |
| نص أساسي | `ink` | `#16202B` |
| نص ثانوي | `ink-soft` | `#5B6875` |
| نص باهت/placeholder | `ink-faint` | `#96A1AC` |
| حدود | `line` / `line-soft` | `#E4E8EC` / `#EEF1F4` |
| خلفية الصفحة | `page` | `#F7F8FA` |
| خلفية البطاقات | `surface` | `#FFFFFF` |
| خلفية سطح المكتب خارج الإطار | `backdrop` | `#EEF1F5` |

## Typography

خط Cairo، أوزان 400–800. الأرقام (أرقام المعاش، المعاملات، التواريخ) تُعرض دائماً
بصنف `tabular-nums` لمحاذاة رأسية ثابتة في الجداول والقوائم.

## Spacing Scale
4, 8, 12, 16, 24, 32, 48px

## Radius
- sm: 8px (الأزرار والحقول)
- md: 12px
- lg: 16–20px (البطاقات)

## Elevation
ظلال مموّهة بلون العلامة بدلاً من أسود شفاف عام — `shadow-card` للبطاقات المسطّحة،
`shadow-raised` للعناصر البارزة، `shadow-shell` لإطار التطبيق على الشاشات الكبيرة.

## استجابة الشاشات (Responsive)
التطبيق مصمَّم أساساً كتطبيق هاتف بعمود واحد. من مقاس `md` فأعلى، يُعرض المحتوى داخل
إطار بعرض أقصى `480px` مُتمركز على خلفية محايدة (`PageShell`)، بدل تمدد تصميم الهاتف
على كامل عرض المتصفح. الشريط السفلي (`BottomNav`) مثبّت بنفس العرض الأقصى ليبقى
محاذياً للإطار على كل المقاسات.
